{
    "compilerOptions": {
      "target": "ES6",                         // Define a versão do JavaScript para o qual o TypeScript será compilado.
      "module": "commonjs",                    // O Node.js usa o CommonJS para modularização.
      "strict": true,                          // Ativa verificações estritas de tipagem.
      "esModuleInterop": true,                 // Habilita a interoperabilidade entre módulos ES6 e CommonJS.
      "skipLibCheck": true,                    // Ignora a verificação de tipos das bibliotecas.
      "outDir": "./dist",                      // Define o diretório de saída dos arquivos compilados.
      "rootDir": "./src"                       // Define o diretório onde os arquivos de origem estarão.
    },
    "include": ["src/**/*.ts"],                // Inclui todos os arquivos TypeScript no diretório "src".
    "exclude": ["node_modules"]                // Exclui a pasta "node_modules".
  }
  